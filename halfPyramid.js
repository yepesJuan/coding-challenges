//  Inside of the homework folder, create another file named function and do this:

// 1. Write a function that adds two numbers together

console.log("lol")

function addTwoNumbers(a,b) {
    return a + b 
}
let result = addTwoNumbers(10,12)
console.log(result)